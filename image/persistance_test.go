package image

import (
	"testing"
)

func TestToPersistance(t *testing.T) {
	// image := NewImage(nil, "docker.io", "alpine", "3.12")
	// report, err := image.Scan()
	// require.NoError(t, err)
	// require.NotNil(t, report)

	// persistance, err := image.ToPersistance("alpine")
	// require.NoError(t, err)
	// require.NotNil(t, persistance)
	// require.Equal(t, "alpine", persistance.Name)
	// results := persistance.Vulncheck.Results
	// require.NotEmpty(t, results)
	// require.Equal(t, "CRITICAL", results[0].Severity)
}
